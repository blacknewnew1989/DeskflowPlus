# Starter 代码许可证

`starter/` 中 C++/CMake 文件使用：

```text
GPL-2.0-only WITH LicenseRef-OpenSSL-Exception
```

这里附带 GPL-2.0-only 正文和 Deskflow v1.26.0 使用的 OpenSSL 例外文本，便于把 starter 合入上游仓库后通过 REUSE/许可证检查。

本开发包不包含 Deskflow 上游源码。真正合入时仍须保留上游完整的 `LICENSE`、`LICENSES/`、`REUSE.toml`、版权头和第三方声明，并以 pinned tag 的真实文件级声明为准。
