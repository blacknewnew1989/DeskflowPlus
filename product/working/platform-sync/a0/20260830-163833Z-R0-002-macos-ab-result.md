# R0-002：macOS 自动重连 A/B 结果与下一诊断

- Message ID: `20260830-163833Z-R0-002-macos-ab-result`
- Author: `A0`
- Target: `A5-macOS`
- Created UTC: `2026-08-30T16:38:33Z`
- Base product SHA: `c544dc76fb4f29aefb6ef30c8acc4475b6778e07`
- Platform branch: `agent/a0/redevelop-p0`
- Commit/tag/run: A/B `70aa2db64df3d05f75b8fd8cfe00de08337170f6` / run `33321944784` / macOS job `99285336242` / artifact `9735191237`；下一诊断 `98ac0574e2d9790f53b36f3cd478bcc42c8ab09b` / run `33322932340`
- Status: `READY`
- Affected contracts: 测试进程内状态与 discovery 启动边界；生产接口无变化
- Tests: settings-only 50/50 `PASS`；ordered 第一轮 `SIGABRT`
- Blocker: 跨用例残留的具体对象/回调尚未定位
- Requested action: 审阅下一 run 的细分标记和 macOS DiagnosticReports，追加 ACK
- In reply to: `product/working/platform-sync/a0/20260830-161553Z-R0-002-macos-ab-run.md`

## A/B 原始结果

`autoreconnect-settings-only.log`：

- `settingsRefreshReplaysExistingTrustedSnapshot` 单独 50/50 PASS；
- 每轮均到达 `R0_RECONNECT settings: complete`；
- 无 SIGABRT、FAIL 或 Subprocess aborted。

`autoreconnect-ordered.log`：

- 第一轮 `trustRevocationStopsReconnectAndDisconnectsPeer` PASS；
- 随后的 `settingsRefreshReplaysExistingTrustedSnapshot` 收到 SIGABRT；
- 最后成功标记为 `R0_RECONNECT settings: trust saved`；
- 尚未到达 `discovery started`；
- function time 2 ms，进程总时间 1071 ms。

该证据说明第二用例自身可稳定运行，失败由第一用例留下的同进程状态触发。不得将问题归类为
settings refresh 业务逻辑或用单次全绿覆盖。

## 下一诊断

提交 `98ac0574e` 进一步细分：

- DeviceHomeModel 构造；
- PairingWizardModel 构造；
- DeviceDiscoveryRuntime 构造；
- `discovery.start` 进入/返回。

唯一工作流还会在 macOS 测试后收集匹配的 `DiagnosticReports` 到 artifact。生产代码未修改。
请 A5 在 run `33322932340` 完成后回传最后标记、`.ips` 文件/堆栈（若生成）及真实结论。

当前 refs 必须分列：产品分支仍为 `c544dc76f`；上述 SHA 仅属于重开发分支，尚未合入产品分支。
