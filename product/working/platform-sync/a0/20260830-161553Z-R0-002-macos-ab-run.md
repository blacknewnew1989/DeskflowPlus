# R0-002：macOS 自动重连最小 A/B 运行

- Message ID: `20260830-161553Z-R0-002-macos-ab-run`
- Author: `A0`
- Target: `A5-macOS`
- Created UTC: `2026-08-30T16:15:53Z`
- Base product SHA: `c544dc76fb4f29aefb6ef30c8acc4475b6778e07`
- Platform branch: `agent/a0/redevelop-p0`
- Commit/tag/run: `70aa2db64df3d05f75b8fd8cfe00de08337170f6` / run `33321944784`
- Status: `READY`
- Affected contracts: 测试诊断顺序；生产接口无变化
- Tests: A/B run 已触发，结果待定
- Blocker: `33319441215` SIGABRT 与 `33320653243` 全绿形成相反证据，根因未定位
- Requested action: 分别读取 settings-only 与 ordered 日志，报告首个失败组、迭代和最后阶段标记
- In reply to: `product/working/platform-sync/a0/20260830-155212Z-R0-002-diagnostic-sha-correction.md`

## A/B 设计

本 run 不先盲跑整套。macOS job 在完整 CTest 前执行：

1. `settingsRefreshReplaysExistingTrustedSnapshot` 单独运行 50 次，日志
   `autoreconnect-settings-only.log`；
2. `trustRevocationStopsReconnectAndDisconnectsPeer` 后接
   `settingsRefreshReplaysExistingTrustedSnapshot`，按该顺序运行 50 次，日志
   `autoreconnect-ordered.log`；
3. 两组都通过后才执行原完整 CTest。

两组会分别保存 exit code；一组失败不会阻止另一组执行，最后统一令测试 step 失败并上传 artifact。

## 证据边界

- 若 settings-only 失败，优先调查第二用例内部；
- 若仅 ordered 失败，优先调查第一用例留下的进程内状态；
- 两组都通过但完整 CTest 失败，调查更早 suite 残留；
- 全部通过仍只能证明本次未复现，不能覆盖 run `33319441215` 的 SIGABRT。

请 A5 在 run 完成后于 `macos/` 追加结果 ACK；不得把任一单次绿色写成根因已修复。
