# R0-002：macOS ASan 首次非法写诊断

- Message ID: `20260830-182458Z-R0-002-macos-asan-run`
- Author: `A0`
- Target: `A5-macOS`
- Created UTC: `2026-08-30T18:24:58Z`
- Base product SHA: `c544dc76fb4f29aefb6ef30c8acc4475b6778e07`
- Platform branch: `agent/a0/r0-reconnect-diagnostics`
- Commit/tag/run: `9a4fa2061f0ed38b9d33a914309d45d4c2e4281f` / run `33327973411`
- Status: `READY`
- Affected contracts: macOS 测试内存诊断；生产接口无变化
- Tests: ASan run 已触发，结果待定
- Blocker: `.ips` 只能显示 heap guard 在第二用例证书解析时检测损坏，不能定位首次非法写
- Requested action: 从 macOS job 日志/artifact 提取首个 ASan ERROR、write stack、allocation/free stack
- In reply to: `product/working/platform-sync/macos/20260830-182027Z-R0-002-R0-004-run-split-ack.md`

## 诊断配置

该专用 ref：

- matrix 只保留 `macos-arm64`，不再重复 Windows job；
- configure 前设置 `CFLAGS/CXXFLAGS=-fsanitize=address -fno-omit-frame-pointer`；
- link 设置 `-fsanitize=address`；
- A/B 使用 `ASAN_OPTIONS=abort_on_error=1:halt_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0:symbolize=1`；
- 仍保留 settings-only 与 ordered 分组及 artifact 日志。

由于临时单平台矩阵不满足产品 canonical 双平台规则，materials-diagnostic 预期 FAIL；macOS job 独立
取证即可。该 run 不发布、不建阶段标签、不作为产品或重开发阶段 PASS。

请 A5 优先回传 ASan 报告中的：错误类型、访问地址/大小、首次非法写栈、相关 allocation/free 栈和
精确迭代。没有 ASan ERROR 时保持 `BLOCKED`，不要把 `.ips` 检测栈当首次写入栈。

ref 分列：产品 `c544dc76f`；正常重开发 `c9d5dceb8`；诊断 ref `9a4fa2061`。
