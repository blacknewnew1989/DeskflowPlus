# WIN-003 Windows package productization handoff

## Scope

- Branch: `agent/a4/windows-packaging`
- Baseline: `7cafbf50e49e12976c7b81390a7a30f5e2fd3444`
- Product package commit: `bc8dc16e3`
- Artifact contract commit: `d32bd099c`
- Product state and task board were not modified.

## Product contract

- Product, vendor, package ID, service name, and stable MSI UpgradeCode come from `product/branding/RelayDeskBrand.cmake`.
- Internal Windows binary packages are explicitly named `*-unsigned-portable.7z` and `*-unsigned.msi`.
- The portable archive uses `settings/RelayDesk.conf`, removes the daemon, and documents that deleting its folder deletes its local settings.
- The MSI retains configuration, trust records, and transfer history outside the installation directory during ordinary upgrade and uninstall. The package README makes that boundary explicit.
- MSI service, finish-page, and firewall strings use RelayDesk product identity. Firewall exceptions use only the Windows private profile.
- The stable UpgradeCode is `50C1FCAB-2BF8-447C-806D-A53C21C6A237`; per-build ProductCode remains generated by CPack/WiX to support major upgrades.
- `package-windows.ps1` now rejects a missing portable archive, rejects missing MSI when WiX is available, and verifies the unsigned manifest contract after collection.

## Windows validation

Environment:

- Windows 11 `10.0.26200.0`
- CMake/CPack `3.30.5`
- WiX `5.0.2`
- Qt MinGW GCC `13.1.0` for the MSI custom-action DLL

Results:

- Python packaging tests: 6 PASS.
- PowerShell artifact-contract test: PASS, including missing-MSI rejection.
- Branding validation: PASS.
- Development-package validation: PASS.
- Real CPack 7Z generation: PASS.
  - `relaydesk-1.26.0-test-win-x64-unsigned-portable.7z`
  - Contents include `deskflow.exe`, `deskflow-core.exe`, `README-PORTABLE.txt`, and `settings/RelayDesk.conf`.
  - Contents exclude `deskflow-daemon.exe`.
- Real WiX generation: PASS.
  - `relaydesk-1.26.0-test-win-x64-unsigned.msi`
  - `wix msi validate`: PASS with non-fatal CPack ICE61/69/90 warnings.
  - Decompiled MSI confirms product `RelayDesk`, stable UpgradeCode, service `RelayDesk`, private firewall profile value `2`, and productized finish action.
- MSI administrative extraction: PASS (`msiexec /a`, exit 0), producing `PFiles64/RelayDesk` with GUI, core, and daemon payloads without installing the harness service.

The full RelayDesk application build/package is `NOT_RUN` locally because the installed Visual Studio Build Tools instance lacks the C++ workload and the local Qt/vcpkg application toolchain is incomplete. The generated package harness exercised the actual CPack 7Z generator, actual WiX extensions, actual product templates, custom-action compilation, MSI validation, decompilation, and administrative extraction. The normal Windows Actions runner remains the full application build path after A0 integrates these commits.

An actual clean install/major upgrade/uninstall cycle is `NOT_RUN` for the package harness because its executable payloads are deliberate dummy files and installing its automatic service/firewall entries would mutate the workstation without validating the real application. The MSI tables and administrative image validate the intended boundary; TEST-005 should execute the destructive lifecycle on the full Actions-built package in a disposable Windows runner/VM.

## Integration order

```text
bc8dc16e3
d32bd099c
<this handoff commit>
```
